import momo from "./momo.js";

const section = document.querySelector("section");
// momo({ duration: "2000", curve: "cubic-bezier(0.65, 0, 0.35, 1)" });
momo("h1", { duration: "2000", curve: "cubic-bezier(0.65, 0, 0.35, 1)" });
